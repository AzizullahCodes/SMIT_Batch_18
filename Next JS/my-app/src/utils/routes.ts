const publicRoutes = ["/signup", "/login"];

const privateRoutes = [
    "/",
    "/about",
    "/contact",
    "/contact/profile",
    "/services",
    "/users"
];

export { publicRoutes, privateRoutes };